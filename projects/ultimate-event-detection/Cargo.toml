[package]
name = "ultimate-event-detection"
version = "0.1.0"
edition = "2021"

[features]
metal = ["dep:cc"]

[dependencies]

[build-dependencies]
cc = { version = "1.0", optional = true }

[dev-dependencies]
