# Sprinting Boxes Configuration

# Root directory where video files are stored
SPRINTING_BOXES_VIDEO_ROOT=/path/to/videos

# Root directory where output artifacts will be saved
SPRINTING_BOXES_OUTPUT_ROOT=/path/to/output

# Port to bind the server to (optional, defaults to 12206)
# SPRINTING_BOXES_PORT=12206

# Host to bind the server to (optional, defaults to 127.0.0.1)
# SPRINTING_BOXES_HOST=127.0.0.1
